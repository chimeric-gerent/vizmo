class EmployeeData {
  final int id;
  final String name, email, phone, country, createdAt, avatar, birthday;
  final List department;
  EmployeeData(this.id, this.name, this.avatar, this.email, this.phone,
      this.birthday, this.country, this.createdAt, this.department);
}
